<%= form_with(model: projects_user, local: true) do |form| %>
  <% if projects_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(projects_user.errors.count, "error") %> prohibited this projects_user from being saved:</h2>

      <ul>
        <% projects_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
