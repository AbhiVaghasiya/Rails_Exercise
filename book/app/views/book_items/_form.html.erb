<%= form_with(model: book_item, local: true) do |form| %>
  <% if book_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book_item.errors.count, "error") %> prohibited this book_item from being saved:</h2>

      <ul>
        <% book_item.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :author_id %>
    <%= form.number_field :author_id %>
  </div>

  <div class="field">
    <%= form.label :publisher_id %>
    <%= form.number_field :publisher_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
