<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :User %>
    <%= form.text_field :User %>
  </div>

  <div class="field">
    <%= form.label :Book_id %>
    <%= form.text_field :Book_id %>
  </div>

  <div class="field">
    <%= form.label :Book_name %>
    <%= form.text_field :Book_name %>
  </div>

  <div class="field">
    <%= form.label :Book_cost %>
    <%= form.text_field :Book_cost %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
